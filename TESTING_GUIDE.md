# دليل الاختبار السريع

## 🧪 اختبار نظام البريد الإلكتروني والاشتراكات

### المتطلبات الأولية

1. **تأكد من إعداد المتغيرات البيئية:**
   ```env
   EMAIL_USER=adelvip222222@gmail.com
   EMAIL_PASSWORD=1234567890mmmm@@@@
   NEXT_PUBLIC_APP_URL=http://localhost:3000
   ```

2. **تشغيل المشروع:**
   ```bash
   npm run dev
   ```

---

## 1️⃣ اختبار التسجيل والتحقق من البريد

### الخطوات:

1. **افتح المتصفح:**
   ```
   http://localhost:3000
   ```

2. **سجل مستخدم جديد:**
   - اضغط على "تسجيل" أو املأ نموذج التسجيل
   - أدخل:
     - الاسم: `مستخدم تجريبي`
     - البريد: `test@example.com` (أو بريدك الحقيقي)
     - كلمة المرور: `123456`

3. **تحقق من البريد الوارد:**
   - افتح بريد `test@example.com`
   - ابحث عن رسالة من `adelvip222222@gmail.com`
   - العنوان: "تأكيد البريد الإلكتروني - 4IT"

4. **اضغط على رابط التحقق:**
   - سيتم توجيهك إلى `/verify-email?token=xxx`
   - يجب أن ترى رسالة "تم التفعيل بنجاح!"
   - سيتم توجيهك تلقائياً إلى صفحة تسجيل الدخول

5. **سجل الدخول:**
   - استخدم البريد وكلمة المرور
   - يجب أن تدخل إلى `/onboarding`

### ✅ النتيجة المتوقعة:
- ✅ تم إرسال البريد بنجاح
- ✅ تم التحقق من الحساب
- ✅ يمكن تسجيل الدخول

---

## 2️⃣ اختبار استرداد كلمة المرور

### الخطوات:

1. **اذهب إلى صفحة تسجيل الدخول:**
   ```
   http://localhost:3000/admin-login
   ```

2. **اضغط على "نسيت كلمة المرور؟":**
   - سيتم توجيهك إلى `/forgot-password`

3. **أدخل بريدك الإلكتروني:**
   - أدخل: `test@example.com`
   - اضغط "إرسال رابط إعادة التعيين"

4. **تحقق من البريد الوارد:**
   - افتح بريد `test@example.com`
   - ابحث عن رسالة "استرداد كلمة المرور - 4IT"

5. **اضغط على رابط إعادة التعيين:**
   - سيتم توجيهك إلى `/reset-password?token=xxx`
   - أدخل كلمة المرور الجديدة: `newpassword123`
   - أعد إدخالها للتأكيد
   - اضغط "حفظ كلمة المرور"

6. **سجل الدخول بكلمة المرور الجديدة:**
   - استخدم `test@example.com` و `newpassword123`

### ✅ النتيجة المتوقعة:
- ✅ تم إرسال بريد استرداد كلمة المرور
- ✅ تم إعادة تعيين كلمة المرور بنجاح
- ✅ يمكن تسجيل الدخول بكلمة المرور الجديدة

---

## 3️⃣ اختبار إنشاء موقع واشتراك تجريبي

### الخطوات:

1. **سجل الدخول:**
   - استخدم حساب مفعّل

2. **املأ نموذج Onboarding:**
   - الخطوة 1: اسم الموقع: `شركة الاختبار`
   - الخطوة 2: نوع الموقع: `شركة تقنية`
   - الخطوة 3: اللون: `أزرق`
   - اضغط "إنشاء الموقع 🚀"

3. **تحقق من إنشاء الاشتراك:**
   - يجب أن تدخل إلى `/admin/dashboard`
   - اذهب إلى `/admin/subscription`

4. **تحقق من تفاصيل الاشتراك:**
   - الخطة: `تجريبي`
   - الحالة: `نشط`
   - الأيام المتبقية: `30 يوم`

### ✅ النتيجة المتوقعة:
- ✅ تم إنشاء الموقع بنجاح
- ✅ تم إنشاء اشتراك تجريبي (30 يوم)
- ✅ يظهر في صفحة الاشتراك

---

## 4️⃣ اختبار تجديد الاشتراك

### الخطوات:

1. **اذهب إلى صفحة الاشتراك:**
   ```
   http://localhost:3000/admin/subscription
   ```

2. **اختر خطة:**
   - اضغط على "اختر هذه الخطة" تحت أي خطة (مثلاً: شهري)

3. **تحقق من التجديد:**
   - يجب أن ترى رسالة "تم تجديد الاشتراك بنجاح!"
   - الصفحة تُحدّث تلقائياً
   - الخطة الجديدة تظهر في "الاشتراك الحالي"

### ✅ النتيجة المتوقعة:
- ✅ تم تجديد الاشتراك
- ✅ تم تحديث تاريخ الانتهاء
- ✅ الحالة: `نشط`

---

## 5️⃣ اختبار Cron Job

### الخطوات:

1. **افتح Terminal:**

2. **نفذ الأمر:**
   ```bash
   curl -X GET "http://localhost:3000/api/cron/subscription-check?secret=your-secret-token"
   ```

3. **تحقق من الاستجابة:**
   ```json
   {
     "success": true,
     "message": "Subscription check completed",
     "updatedSubscriptions": 0,
     "notificationsSent": 0
   }
   ```

### ✅ النتيجة المتوقعة:
- ✅ الـ endpoint يعمل
- ✅ يتم تحديث الاشتراكات المنتهية
- ✅ يتم إرسال التنبيهات

---

## 6️⃣ اختبار انتهاء الاشتراك (محاكاة)

### الخطوات:

1. **افتح MongoDB Compass أو mongosh:**

2. **ابحث عن اشتراكك:**
   ```javascript
   db.subscriptions.find({ userId: ObjectId("your-user-id") })
   ```

3. **عدّل تاريخ الانتهاء ليكون في الماضي:**
   ```javascript
   db.subscriptions.updateOne(
     { userId: ObjectId("your-user-id") },
     { $set: { 
       endDate: new Date("2024-01-01"),
       status: "expired"
     }}
   )
   ```

4. **حاول الدخول إلى لوحة التحكم:**
   - يجب أن يتم توجيهك إلى `/admin/subscription?expired=true`
   - ترى رسالة تحذير حمراء

5. **حاول زيارة الموقع العام:**
   ```
   http://localhost:3000/s/your-slug
   ```
   - يجب أن ترى صفحة "الموقع غير متاح حالياً"

6. **جدد الاشتراك:**
   - من صفحة `/admin/subscription`
   - اختر خطة جديدة
   - يجب أن يعود كل شيء للعمل

### ✅ النتيجة المتوقعة:
- ✅ يتم حظر الوصول للوحة التحكم (ما عدا صفحة الاشتراك)
- ✅ الموقع العام يظهر رسالة "غير متاح"
- ✅ بعد التجديد، يعود كل شيء للعمل

---

## 7️⃣ اختبار Super Admin

### الخطوات:

1. **أنشئ super-admin:**
   ```bash
   npm run tsx scripts/create-admin.ts
   ```
   - البريد: `admin@4it.com`
   - كلمة المرور: `Admin@123456`

2. **سجل الدخول:**
   ```
   http://localhost:3000/admin-login
   ```

3. **تحقق من التوجيه:**
   - يجب أن تدخل مباشرة إلى `/super-admin/dashboard`
   - لا يتم توجيهك إلى `/onboarding`
   - لا يتم فحص الاشتراك

### ✅ النتيجة المتوقعة:
- ✅ Super-admin يتجاوز جميع الفحوصات
- ✅ يدخل مباشرة إلى لوحة التحكم الخاصة

---

## 🐛 استكشاف الأخطاء

### البريد لا يصل:

1. **تحقق من Console:**
   ```bash
   # ابحث عن أخطاء في terminal
   Error sending verification email: ...
   ```

2. **تحقق من بيانات Gmail:**
   - تأكد من `EMAIL_USER` و `EMAIL_PASSWORD` صحيحة
   - تأكد من استخدام "كلمة مرور التطبيق" وليس كلمة المرور العادية

3. **تحقق من Spam:**
   - قد يكون البريد في مجلد Spam

### الاشتراك لا يُنشأ:

1. **تحقق من Logs:**
   ```bash
   # في terminal
   Failed to create trial subscription
   ```

2. **تحقق من MongoDB:**
   ```javascript
   db.subscriptions.find()
   ```

3. **تحقق من Site:**
   ```javascript
   db.sites.find({ userId: ObjectId("your-user-id") })
   ```

### Cron Job لا يعمل:

1. **تحقق من CRON_SECRET:**
   - يجب أن يكون نفسه في `.env` وفي الـ URL

2. **تحقق من الاتصال بقاعدة البيانات:**
   ```bash
   # في terminal
   Error checking subscription: ...
   ```

---

## 📊 قائمة التحقق النهائية

- [ ] تسجيل مستخدم جديد يعمل
- [ ] بريد التحقق يصل
- [ ] التحقق من البريد يعمل
- [ ] استرداد كلمة المرور يعمل
- [ ] إنشاء موقع يعمل
- [ ] اشتراك تجريبي يُنشأ تلقائياً
- [ ] صفحة الاشتراك تعرض البيانات الصحيحة
- [ ] تجديد الاشتراك يعمل
- [ ] Cron job يعمل
- [ ] انتهاء الاشتراك يحظر الوصول
- [ ] Super-admin يتجاوز الفحوصات

---

## 🎉 إذا نجحت جميع الاختبارات

مبروك! النظام يعمل بشكل صحيح. يمكنك الآن:

1. **Deploy إلى Production:**
   ```bash
   vercel --prod
   ```

2. **إعداد Cron Job في Vercel:**
   - يتم تفعيله تلقائياً من `vercel.json`

3. **مراقبة النظام:**
   - تحقق من logs في Vercel Dashboard
   - راقب البريد الإلكتروني
   - راقب الاشتراكات في MongoDB

---

## 📞 الدعم

إذا واجهت أي مشاكل:

1. راجع `EMAIL_SUBSCRIPTION_SYSTEM.md` للتوثيق الكامل
2. راجع `IMPLEMENTATION_SUMMARY.md` لملخص التنفيذ
3. تحقق من logs في console
4. تحقق من قاعدة البيانات

---

**ملاحظة:** هذا الدليل للاختبار في بيئة التطوير. في الإنتاج، تأكد من:
- استخدام HTTPS
- تأمين CRON_SECRET
- مراقبة الأداء
- عمل backup دوري
